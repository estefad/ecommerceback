import dotenv from 'dotenv'

dotenv.config({

})

export const objetConfig = {
    port: process.env.PORT
}

//ACA SE IMPORTAN LAS VARIABLES DE ENTORNO
 //manejamos diferentes entornos de trabajo segun lo que se requiera
    //en el archivo .env se definen las variables de entorno
    